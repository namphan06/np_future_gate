import 'package:flutter/material.dart';

class PartnershipRequestsScreen extends StatelessWidget {
  const PartnershipRequestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Yêu cầu liên kết'),
      ),
      body: const Center(
        child: Text('Partnership Requests Screen - Coming soon'),
      ),
    );
  }
}
