import 'package:flutter/material.dart';

class SchoolPartnershipApplicantsScreen extends StatelessWidget {
  const SchoolPartnershipApplicantsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ứng viên tin liên kết'),
      ),
      body: const Center(
        child: Text('Partnership Applicants Screen - Coming soon'),
      ),
    );
  }
}
